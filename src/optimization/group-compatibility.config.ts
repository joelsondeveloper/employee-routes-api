export const GROUP_COMPATIBILITY_CONFIG = {
  directionWeight: 0.15,
  proximityWeight: 0.20,
  distanceWeight: 0.10,
  roadWeight: 0.55,

  maxDirectionDifference: 90,
  maxProximityKm: 10,
  maxDistanceDifferenceKm: 20,
};